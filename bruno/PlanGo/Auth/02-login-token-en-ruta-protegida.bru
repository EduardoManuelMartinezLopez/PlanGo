meta {
  name: Usar token en ruta protegida (GET /me)
  type: http
  seq: 2
}

get {
  url: {{base_url}}/api/me
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{token}}
}

assert {
  res.status: eq 200
}
