<?php

namespace App\Http\Controllers;

/**
 * Esta es la clase base de la que heredan TODOS los controladores de
 * Laravel (DestinoController, AuthController, etc. — con "extends
 * Controller" arriba de cada uno). Es parte del esqueleto por default
 * de Laravel y se me había olvidado incluirla — por eso salía el error
 * "Class App\Http\Controllers\Controller not found".
 */
abstract class Controller
{
    //
}
